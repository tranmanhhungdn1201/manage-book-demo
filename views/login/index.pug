extends ../common/common
block content
  .container
    h1.text-center Login
    if errors
      each error in errors
        .alert.alert-danger=error
    form(action="", method="POST")
      .form-group
        label Email:
        input.form-control(type="text", name="email", value=value ? value.email : "")
      .form-group
        label Password:
        input.form-control(type="password", name="password", value=value ? value.age : "")
      .form-group
        button.btn.btn-success Login