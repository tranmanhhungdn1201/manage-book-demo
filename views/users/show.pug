extends ../common/common
block content
  .container
    h1.text-center Show user
    .form-group
      label Name: 
      =user.name
    .form-group
      label Age: 
      =user.age
    .form-group
      a.btn.btn-success(href="/users/" + user.id +"/edit") Edit